# Database Configuration
# Note: The application uses the 'logs' database, not 'jobmanager'
# The 'logs' database contains the production_planner_paths, runlists, and runlist_impositions tables
DB_HOST=localhost
DB_PORT=5432
DB_NAME=logs
DB_USER=postgres
DB_PASSWORD=your_password_here

# Server Configuration
PORT=3001
CORS_ORIGIN=http://localhost:5173

# Node Environment
NODE_ENV=development
